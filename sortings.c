#include<stdio.h>
// int selection_sort(int n,int b[]);
 void print(int , int []);
 int main (){
 	int n,i;
 	printf("enter the size of the array:");
 	scanf("%d",&n);
 	int a[n];
 	for(i=0;i<n;i++){
 		int j;
 		printf("enter a number:");
 		a[i] = j;
 		print(n,a);
	 }
   void print(int n,int b[]){
   	   int i;
   	for(i=0;i<n;i++){
   		printf("%d",b[i]);
	   }
   } 
	 
 }
